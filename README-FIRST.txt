START HERE
==========

Open INSTALL.md.

The two values you will need are:

My filters:
  twitch.tv##+js(veno-twitch-stable)

Local Windows userResourcesLocation:
  http://127.0.0.1:8765/veno-twitch-stable.js

For the recommended hosted setup, upload veno-twitch-stable.js unchanged and use its raw HTTPS URL instead.

Optional integrity check (Node.js required):
  Run-Validation.cmd

For an exact source review:
  AUDIT-DIFF.patch
